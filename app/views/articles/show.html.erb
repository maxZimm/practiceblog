<h1>
  <strong>Title:</strong>
  <%= @article.title %>
</h1>

<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<h4>Comments</h4>
<%= render @article.comments %>

<h4>Add your comment</h4>
<%= render 'comments/form' %>

<%= link_to 'Back', articles_path %> |
<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Delete', article_path(@article), method: :delete, data:{ confirm: 'Are you certain?' } %>
